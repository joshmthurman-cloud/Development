{
  "name": "terminal-builder",
  "version": "1.0.0",
  "description": "Terminal Builder from VAR Sheets - Web application for processing VAR sheets and creating TPNs in STEAM",
  "main": "server/server.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && node server.js",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd server && node server.js",
    "install:all": "npm install && cd server && npm install --production && cd ../client && npm install",
    "deploy": "npm run build && npm start"
  },
  "keywords": ["terminal", "VAR", "STEAM", "TPN"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}



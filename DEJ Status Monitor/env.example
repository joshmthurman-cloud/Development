# Copy this file to .env and fill in your values:
#   cp env.example .env
# Then edit .env with your real API key and restart the app.

# Required for login sessions
SECRET_KEY=change-this-to-a-random-string

# Required for account-creation and password-reset emails (SendGrid)
SENDGRID_API_KEY=
NOTIFICATION_EMAIL=josh.thurman@curbstone.com
NOTIFICATION_SENDER_NAME=Terminal Status Monitor

# Optional: use SMTP instead of SendGrid
# EMAIL_PROVIDER=smtp
# NOTIFICATION_EMAIL_PASSWORD=
# SMTP_SERVER=smtp.office365.com
# SMTP_PORT=587

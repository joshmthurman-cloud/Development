# Production Environment Variables
# Copy this file to .env and update with your production values

# Server Configuration
PORT=8092
HOST=0.0.0.0
NODE_ENV=production

# CORS Configuration
# Comma-separated list of allowed origins (e.g., "http://10.200.0.235:8092,http://localhost:8092")
CLIENT_URL=http://10.200.0.235:8092

# Session Security
# IMPORTANT: Generate a strong random secret for production!
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-strong-random-secret-key-here-change-this

# STEAM API Configuration (Optional - can also be set in database)
# These are defaults, but the database config takes precedence
STEAM_API_URL=https://dvmms.com/steam/api/ws/VDirectAccess.asmx
STEAM_WSDL_URL=https://dvmms.com/steam/api/ws/VDirectAccess.asmx?WSDL
STEAM_USERNAME=apihandler
STEAM_PASSWORD=gitBMk!qhKqR&KC5

# SPIn Proxy URL (Optional)
# SPIN_PROXY_URL=https://spinpos.net/spin
